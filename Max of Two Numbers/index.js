let number = [1, 5];
if (number[0] > number[1]) let big = number[0];
  else if (number[0] < number[1]) let big = number[1];
  console.log("The biggest number is" + big);
  else console.log("The numbers are the same");
  
